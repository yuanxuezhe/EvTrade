# 2026-08-22 工作日志

## 策略开发-新建脚本即时创建

按知识库驱动开发流程实现：点击"新建脚本"按钮直接在后端创建脚本并显示到列表，不再等用户手动保存。

### 改动内容
- **知识库先改**：`知识库/后端服务/策略引擎/脚本策略模块.md` + `知识库/前端/页面/策略开发与运行.md` 增加新端点与流程描述
- **后端**：`server/services/script_strategy/scripts.py` 新增 `auto_create_script()`（自动命名 new_strategy → new_strategy01 → 02…）；`server/api/script_strategy/scripts.py` 新增 `POST /scripts/new` 端点 + 提取 `_load_default_template()` 复用函数；`__init__.py` 导出新函数
- **前端**：`client/src/api/script_strategy.js` 加 `newScript()` 方法；`client/src/views/ScriptDev.vue` 的 `onCreate()` 改为直接调 API 创建 + 刷新列表 + 选中
- **测试**：新增 `server/tests/test_script_new.py`（4 case：首次/重名递增/三次递增/未认证）

### 测试结果
- `test_script_new.py` 4 passed
- `test_script_strategy_compile.py` 3 passed（回归无破坏）
- `test_strategy_v123_api.py` 6 passed（回归无破坏）

### 改动范围
git diff --stat 确认 7 文件改动，全部在 script_strategy 模块内，无无关文件。

## 编写 Agent 工作指南

在 `知识库/开发流程/Agent工作指南.md` 编写了完整的 Agent 调教文档，覆盖：
- 五条铁律（知识库驱动/先改KB再改代码/范围最小化/测试受控/KB规范）
- 分析阶段（读知识库→读源码→梳理现状）
- 开发阶段（先改KB→再改代码，含后端/前端改动顺序与实战示例）
- 测试阶段（写测试→跑测试，含 pytest 运行须知）
- 收尾阶段（git diff --stat→KB-代码一致性→工作日志）
- 常见陷阱与避坑指南（__init__导出/HTTP方法/el-table数组/PYTEST模式/stale task/子代理中断）
- 工作流程总览图 + 关键路径速查表 + Agent自检清单

同步更新了 `知识库/目录索引.md`（开发流程 2→3 份，总计 64→65 份）。

## 知识库与代码全面一致性审计

派出 7 个子代理分域核查全部 65 份知识库文档与代码的匹配度，不匹配处直接修正知识库（以代码为准，未改任何代码）。共修正约 130 处。

### 审计结果
| 域 | 文档数 | 修正数 | 典型问题 |
|----|--------|--------|----------|
| 交易核心+T0 | 7 | ~32 | T0 路由前缀错（/api/t0-tasks 非 /api/t0/tasks）、撤单可撤状态过时（v91 改 50/55）、balance_task 功能整体描述错误、v84/v118 语义变更未跟进 |
| 策略引擎+行情 | 5 | 13 | 脚本端点数、v131 quote-batch-flush 批量推送未记录、不存在的 depth 端点 |
| 系统管理+鉴权+同步 | 9 | 0 | 全部精确匹配 |
| 后端基础设施 | 11 | 15 | 项目总览大改（21 页面、hqserverd Rust 版、7 WS 频道、MySQL-only）、v131 新配置项、stkpool 两张表漏记 |
| 前端 | 17 | ~45 | 3 个新页面漏记（stkpool×2、ai-analysis）、hydrate 屏障迁至 App.vue、T0Trade 实际 1290 行（非 55K）、v125 起纯回测无实盘 |
| 策略服务+行情服务 | 8 | 8 | hqserver 说明重写为 Rust hqserverd（UDP 直推+批量合并帧）、forward.py 拆分 |
| 脚本工具+数据库+流程 | 6 | 17 | Schema 21 张表（补 stkpool/stkpooldetail）、mock_ticker 链路过时、测试清单更新 |

### 教训
知识库建成仅 6 天就积累了 130 处漂移——主要来自 v84~v131 的多次迭代（hqserver Rust 化、批量推送、stkpool 模块、策略纯回测化）未同步知识库。**今后每次改代码必须同步改知识库**（全局规范铁律），建议定期（如每两周）跑一次一致性审计。
