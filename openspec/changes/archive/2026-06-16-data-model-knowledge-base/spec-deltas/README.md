# Data model knowledge base — spec deltas

本 change 不需要 spec-deltas（新建 cap，spec 内容直接放在 `specs/data-model/spec.md`）。

如需修改现有 cap 引用方式（链接/章节号），按各 cap spec 的 cross-reference 习惯在 `data-model/spec.md` 顶部说明。
