[pytest]
asyncio_mode = auto
testpaths = tests
# tests/server/rpc/test_rpc.py is an integration script that connects to a live RabbitMQ broker.
# It cannot run without a broker; ignored by default.
addopts = --ignore=tests/server/rpc