# 路由与权限

## 对应代码路径

- `client/src/router/index.js`（128 行，全部路由与守卫逻辑所在）
- 关联：`client/src/stores/auth.js`（守卫读取的用户态）、`client/src/api/index.js`（setUnauthorizedHandler 反向注册）

## 功能概述

使用 Vue Router 4 `createWebHistory` 模式。权限通过路由 `meta` 声明：

- `meta.public: true` — 无需登录（仅 /login）
- `meta.blank: true` — 空白布局（App.vue 的 isBlankLayout 判断）
- `meta.requiresAdmin: true` — 仅 admin 角色
- `meta.requiresTrader: true` — admin 或 trader 角色
- 其余路由 — 任意已登录用户（含 viewer）

守卫统一在 `router.beforeEach` 中实现，401 时由 api 层的 `setUnauthorizedHandler` 回调路由跳转。

## 文件清单

| 文件 | 说明 |
|---|---|
| client/src/router/index.js | 路由表 + redirect 兼容 + beforeEach 守卫 + setUnauthorizedHandler 注册 |

## 核心实现

### 路由全表

| 路径 | 组件 | meta 权限 | 说明 |
|---|---|---|---|
| /login | Login.vue | public + blank | 登录页，已登录访问会被弹回 / |
| / | Dashboard.vue | 登录 | 仪表盘（默认页） |
| /trade | Trade.vue | 登录 | 手动交易 |
| /asset | Asset.vue | 登录 | 资金/资产 |
| /profile | Profile.vue | 登录 | 个人信息 |
| /system-init | SystemInit.vue | requiresAdmin | 系统初始化 |
| /system-config | SystemConfig.vue | requiresAdmin | 系统配置 |
| /t0-trade | T0Trade.vue | requiresTrader | T0 交易主表 |
| /script-dev | ScriptDev.vue | requiresTrader | 脚本开发 |
| /script-task | ScriptTask.vue | requiresTrader | 脚本任务（读 ws.lastTaskProgress） |
| /strategy-order | StrategyOrder.vue | requiresTrader | 策略委托 |
| /users | Users.vue | requiresAdmin | 用户管理 |
| /admin/cache/asset | AdminCacheAsset.vue | requiresAdmin | 资产缓存编辑 |
| /admin/cache/positions | AdminCachePositions.vue | requiresAdmin | 持仓缓存管理 |
| /admin/cache/orders | AdminCacheOrders.vue | requiresAdmin | 委托缓存管理 |
| /admin/cache/trades | AdminCacheTrades.vue | requiresAdmin | 成交缓存管理 |
| /admin/stock-config | AdminStockConfig.vue | requiresAdmin | 证券信息管理 |

### 旧路由 redirect 兼容清单

| 旧路径 | 重定向到 |
|---|---|
| /positions | /t0-trade |
| /holdings | /trade |
| /to-management | /t0-trade |
| /t-strategy | /t0-trade |
| /*（未匹配） | / |

### beforeEach 守卫逻辑（顺序短路）

```
1. to.meta.public
   ├─ 已登录 且 to.path === '/login' → next('/')（防重复登录）
   └─ 否则放行
2. 未登录 → next('/login?redirect=' + encodeURIComponent(to.fullPath))
3. to.meta.requiresAdmin && !auth.isAdmin
   → next('/') + ElMessage.warning('需要管理员权限')
4. to.meta.requiresTrader && !auth.isTrader
   → next('/') + ElMessage.warning('需要交易权限')
5. 放行 next()
```

角色判定来自 auth store 的 computed：`isAdmin = role === 'admin'`；`isTrader = admin | trader`；`isViewer` 为只读角色。守卫能读到正确状态的前提是 main.js 的顶层 `await auth.hydrate()` 已完成（启动屏障）。

### 401 与路由联动

```js
// router/index.js 模块加载时注册
setUnauthorizedHandler(() => {
  router.push({ path: '/login', query: { redirect: router.currentRoute.value.fullPath } })
})
```

api/index.js 响应拦截器捕获 401 后：清 token → 调用该 handler → 跳登录并携带当前页，登录成功后可回跳。

## 依赖关系

- router → stores/auth（isAuthenticated/isAdmin/isTrader computed）。
- api/index.js → router（通过 setUnauthorizedHandler 注入的闭包，避免 api 层直接 import router 造成循环依赖）。
- App.vue → router（isBlankLayout 读 route.meta.blank；uiStore.onRouteChange 在切换时关移动端抽屉）。
- WS token 过期（4001）路径：ws_heartbeat._onTokenExpired 动态 import auth + router 跳登录，不经过守卫。

## 修改指南

- **新增需登录页面**：路由表中加一条，默认即受保护；如需 admin/trader 再加对应 meta。
- **新增公开页**：加 `meta: { public: true, blank: true }`，并确认守卫第 1 分支不会把它弹回。
- **废弃旧路径**：保留 redirect 条目（用户书签/外链兼容），不要直接删。
- **改权限模型**（如新增角色）：同步修改 auth.js 的 isTrader/isViewer computed 与守卫第 3/4 步，两处必须一致。
- **改 401 行为**：入口在 api 拦截器，路由侧只维护 handler 注册；不要在守卫里做 401 处理。
